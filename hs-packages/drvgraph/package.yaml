_common: !include ../../package.common.yaml
<<: *common
<<: *flags

name: drvgraph
version: 0.1.0.0

dependencies:
  - ansi-terminal
  - async
  - base
  - bytestring
  - containers
  - directory
  - dlist
  - filepath
  - http-client
  - http-client-tls
  - http-types
  - megaparsec
  - mtl
  - network-uri
  - optics
  - optparse-applicative
  - safe-exceptions
  - stm
  - stm-containers
  - text
  - unliftio

library:
  source-dirs: src

executables:
  drvgraph:
    source-dirs: app
    main: Main.hs
    dependencies:
      - drvgraph

tests:
  test:
    source-dirs: test
    main: Spec.hs
    dependencies:
      - drvgraph
      - file-embed
      - hedgehog
      - tasty
      - tasty-hedgehog
      - tasty-hunit
    ghc-options: *test-ghc-options
    when:
      - condition: "!flag(in-nix-shell)"
        build-tools:
          - tasty-discover:tasty-discover
